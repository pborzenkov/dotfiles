# Don't beeep
setopt nobeep

# Use extended glob format
setopt extended_glob

# Don't allow > to truncate existing files
setopt noclobber

# Notify about background jobs completion right away
setopt notify
